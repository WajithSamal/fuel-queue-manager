import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'fuel-queue-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
